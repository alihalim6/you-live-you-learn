import {StyleSheet} from 'react-native';
import {BASE_COLOR_LIGHT, LIGHT_GRAY, BORDER_WIDTH} from '../constants/AppConstants';

const HomeStyles = StyleSheet.create({
  container: {
    flex: 1
  }
});

export default HomeStyles;